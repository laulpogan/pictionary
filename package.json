{
  "name": "pictionary",
  "version": "0.1.0",
  "description": "The temperature knob Anthropic removed, reimplemented in Gaussian blur. Renders prompts and files as dense PNGs Claude reads as images — smudge for sampling diversity, pack for cheaper tokens.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/laulpogan/pictionary.git"
  },
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "pictionary": "dist/cli.js"
  },
  "files": [
    "dist",
    "skill",
    "bench"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "tsc"
  },
  "dependencies": {
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^20.0.0"
  }
}
